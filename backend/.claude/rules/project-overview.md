---
description: Project identity — Spring Boot 3.2.3 / Java 17 asset-management backend forked from a cinema-manager tutorial, with a separate Angular frontend
alwaysApply: true
---

# Project Overview

- Spring Boot 3.2.3 / Java 17 backend for a personal asset management app (package `tech.getarrays.assetmanager`).
- The Maven artifact is still named `CinemaManager` — this project was forked from a cinema-manager tutorial, and some legacy references remain.
- Not a git repository.
- Frontend is a separate Angular app (dev URL `http://localhost:4200`, used by CORS config in `SimpleCorsFilter` / `app.client.url`).